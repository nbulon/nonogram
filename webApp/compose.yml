services:
  web:
    build: .
    image: nonogram-web:latest
    container_name: nonogram-web
    restart: unless-stopped
    ports:
      # Loopback only: the host's reverse proxy terminates TLS and forwards here
      - "127.0.0.1:${WEB_PORT:-8080}:80"
